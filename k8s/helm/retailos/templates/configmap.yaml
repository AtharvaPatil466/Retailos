apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "retailos.fullname" . }}-config
  labels:
    {{- include "retailos.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
