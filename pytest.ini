[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
norecursedirs = .claude .cursor .codex dashboard node_modules dist build
markers =
    e2e: browser-backed end-to-end tests that require explicit opt-in
